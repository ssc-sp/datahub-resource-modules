# Test with sample project using modules directly

1. Init: `terraform init`
2. Validate: `terraform validate`
3. Apply: `terraform apply -auto-approve`
4. Destroy: `./sample-destroy.ps -Force -NoPrompt`